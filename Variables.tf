box Main() <<< //Welcome back to the Main box!
@ some global \str\:a }} "I love TF!"; //some is the definition keyword, global defines the scope, between \ \ it is the type. }} is the same as == in most languages and === in js.
@ some global \str OR num OR bool\:b }} true; //You can also use union types with the OR keyword.
@ io.opt($/a/); //use $/.../ when you print a variable.
@ io.opt($/b/); 
>>> 
